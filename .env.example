# Секреты для systemd: /etc/cursor-linux-tg-bot/env (создаётся install.sh)
# Для локальной разработки скопируйте в .env в корне проекта

TELEGRAM_BOT_TOKEN=
CURSOR_API_KEY=
# Токен сообщества VK (Управление -> Работа с API -> Ключи доступа)
VK_BOT_TOKEN=
